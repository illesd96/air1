# Project-side deps ONLY. Heavy ML stuff (torch, whisperx, diffusers, etc.)
# is installed in the bootstrap stages with index-URL overrides so the build
# variant (ROCm vs CUDA) is picked correctly.

pymongo>=4.6
requests>=2.31
python-dotenv>=1.0
tqdm>=4.66
Pillow>=10.2
tomli>=2.0; python_version < "3.11"
# ffmpeg must be in PATH (installed by stage 00).
